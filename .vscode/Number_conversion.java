public class Number_conversion {
    public static void Binary_to_decimal(int n){
        int new_num = 0;
        int pow = 0;
        int last_digit = n%10;
        new_num += (last_digit*Math.pow(1,2)); 
    }
    
}
